<?php

return [
    /*
     * プレイヤー/CPU双方の基礎HP。検証用ゲームデザイン仕様書には具体値の記載がないため、
     * プロトタイプ実装時の暫定値としてユーザー確認の上で設定している(調整可能)。
     */
    'base_hp' => 50,

    /*
     * プレイヤー/CPU双方の基礎ST(スタミナ)プール。武器は攻撃のたびにST消費量分だけこのプールを
     * 減らし、足りない場合は攻撃できない(バトル準備画面ワイヤーフレーム仕様7章)。
     * 具体値は未確定のため、既存の武器ステータス(ST消費8〜34、攻撃間隔0.5〜2秒)を踏まえた
     * 仮置きの値(調整可能)。ユーザーへの確認は「時間経過で回復する」方式を採用した上で実施済み。
     */
    'base_st' => 150,

    /*
     * ST自然回復量(1秒あたり)。武器1本あたりの消費速度(ST消費/攻撃間隔 ≒ 14〜20/秒)より
     * 低めに設定することで、長期戦になるほどSTが攻撃頻度のボトルネックになるようにしている仮値。
     */
    'st_regen_per_second' => 12,

    /*
     * ショップの武器オファーが確率で半額になる仕様の抽選確率。企画書に具体値の記載がないため
     * ユーザー確認の上で設定した仮置きの値(調整可能)。素材オファーは対象外。
     * 品揃え決定(初回ロール・「更新」ボタンでのリロール)のたびに、武器オファー1件ごとに抽選する。
     */
    'shop_weapon_discount_chance' => 0.33,
];
